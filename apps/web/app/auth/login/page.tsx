'use client';

// Reuse the unified auth page that already
// supports Login, Register, and Reset Password modes.
import AuthPage from '../page';

export default function LoginPage() {
  return <AuthPage />;
}

